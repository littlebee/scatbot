// which video is being streamed
export const RGB_VIDEO = 0;
export const DEPTH_VIDEO = 1;

// which video overlays to show
export const DEPTH_MAP_OVERLAY = 1;

// 5001 = object detection camera (no depth feed)
// 5002 = use depth camera video and depth feed
export const VIDEO_FEED_PORT = 5002;
